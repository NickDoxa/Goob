__pycache__/
*.py[oc]
*.egg-info/
.venv/
venv/
.env
# Google OAuth credentials — both land in the repo root during calendar
# setup and must never be committed.
google_token.json
client_secret*.json
.pytest_cache/
build/
dist/

# Local-only context (kept out of public repo)
CLAUDE.md
.claude/
# documentation/ is ignored EXCEPT the two prompt files Goob is built on.
# LESSONS.md (bot-written memory) and .backups/ are device-local state and
# must never sync between machines.
documentation/*
!documentation/GOOB.md
!documentation/MOVEMENT.md

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
